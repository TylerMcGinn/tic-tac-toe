#include <stdio.h>
#include <stdlib.h>
#include "ticTacToe.h"


int main(){
    // printf("%c %c\n",chess.horizontal, .vertical);
    game.startGame();
    
    // game.drawBoard();
    system("pause");
    return 0;
}